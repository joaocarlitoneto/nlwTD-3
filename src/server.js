// importar dependência
const express = require('express');

// iniciando lib express
const server = express()

// criar uma rota
server.get('/', () => {
  
})

// ligar o servidor
server.listen(5500)